# A hello world rest api using Spring Boot

Clone the repo.

Open it in IntelliJ IDEA
Run the main function in ```HelloWorldApplication```

Open Chrome tab and check out the ```localhost:8080``` in search bar.
